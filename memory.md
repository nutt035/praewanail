# memory.md — Persistent Project Context
# Update this file whenever a key decision is made or something important is discovered.

---

## Completed Work
> Fill this in as you finish things

- [ ] (nothing recorded yet — add items as you complete them)

## Key Decisions Made

| Decision | Why | Date |
|----------|-----|------|
| Next.js 14 App Router | Modern standard, better performance | [date] |
| Supabase | Auth + DB + Storage in one, free tier | [date] |
| Vercel deployment | Auto-deploy from GitHub, zero config | [date] |

## Environment Variables Required

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
LINE_CHANNEL_ACCESS_TOKEN=
RESEND_API_KEY=
```

## Bugs Found + Fixed

| Bug | Root Cause | Fix Applied |
|-----|-----------|-------------|
| (add when found) | | |

## Installed Packages

```bash
# Add packages here as you install them
# Example:
# npx shadcn-ui@latest init
# npm install zod react-hook-form @hookform/resolvers
```
